apiVersion: v1
kind: Service
metadata:
  name: employees-nodeport
  namespace: web-as
spec:
  type: NodePort
  selector:
    app: employees
  ports:
  - port: 3000           # container port (targetPort)
    targetPort: 3000
    nodePort: 30000      # required by assignment
    protocol: TCP
